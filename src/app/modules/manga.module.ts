import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { Routes, RouterModule } from '@angular/router';

const routes: Routes = [
  // { path: 'load-me', component: TrendingComponent }
];

@NgModule({
  declarations: [
    // TrendingComponent,
    // PopularComponent,
    // ListComponent,
    // GenreComponent
  ],
  imports: [
    CommonModule
  ]
})
export class HomeModule { }
